# Vector2d

A two-dimensional vector

## Properties

1. `x` (Number)—the x component of the vector (read-only)
2. `y` (Number)—the y component of the vector (read-only)

## Methods

See [Vector](../Vector/Vector.design.md) for supported vector methods.

