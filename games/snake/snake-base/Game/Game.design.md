# Game

The main Game class. Called from the module that is tied to the browser

## Constructor

`new Game({ pitch, head, segments, prizes, id })`


## Properties


## Methods

### Public

`emit(event, ...data)` - emits an event with data.
1. `event` (String) - name of the event. Required
2. `...data` (...any) - data to emit with the event. Can be any number of arguments.

