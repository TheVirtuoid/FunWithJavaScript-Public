.selection-container {
    display: flex;
    flex-direction: row;
    gap: 2rem;

    & ul {
        list-style-type: none;
        padding: 0;

        & li {
            margin: 0;
            padding: 0;
        }

    }
}