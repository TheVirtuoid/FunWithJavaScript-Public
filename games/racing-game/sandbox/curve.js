import App from "./app.js";

const generateCurve = (curve, layout) => {
	/*const startPoint = layout.at(-1).endingPosition;
	const { radius, curveDirection } = curve;
	const direction = curveDirection === 'left' ? -1 : 1;
	let controlPoint1;
	let controlPoint2;
	let endPoint;

	switch (degrees) {
		case 90:
			controlPoint1 = { x: startPoint.x, y: startPoint.y, z: startPoint.z + radius / 2 };
			controlPoint2 = { x: startPoint.x, y: startPoint.y, z: startPoint.z + radius / 2 };
			break;
		case 80:
			break;
	}



	{
		type: 'curve',
			startPoint: { x: App.SX, y: App.SY - 39, z: App.SZ + 55 },
		controlPoint1: { x: App.SX, y: App.SY - 39, z: App.SZ + 75 },
		controlPoint2: { x: App.SX - 20, y: App.SY - 39, z: App.SZ + 95 },
		endPoint: { x: App.SX - 40, y: App.SY - 39, z: App.SZ + 95 }
	},*/
}

export { generateCurve}