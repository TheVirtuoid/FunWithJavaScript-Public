{
  "name": "fwjs-racing-game",
  "version": "0.0.1",
  "description": "Fun With JavaScript - Racing Game",
  "author": "TheVirtuoid <thevirtuoid@gmail.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:tdd": "npx cypress open --config-file cypress.config.js",
    "test:tdd:ci": "npx cypress run --config-file cypress.config.js",
    "test:bdd": "npx cypress open --config-file cypress-cucumber.config.js",
    "test:bdd:ci": "npx cypress run --config-file cypress-cucumber.config.js"
  },
  "cypress-cucumber-preprocessor": {
    "e2e": {
      "stepDefinitions": "bdd/step-definitions/*.cy.js"
    }
  }
}
