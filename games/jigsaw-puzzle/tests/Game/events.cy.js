import Game from "../../src/classes/Game/Game.js";

describe('When I receive events from the UI', () => {

});

describe('When i operate on those events in the game', () => {
	let game;
	beforeEach(() => {
		game = new Game();
	});

	it('should start a new game', () => {

	});
	it('should exit/end the game', () => {});
});