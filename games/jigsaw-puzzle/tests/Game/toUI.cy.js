describe('When I send notification to the UI', () => {
	it('should draw the table', () => {});
	it('should draw the piece', () => {});
	it('should move the piece', () => {});

	describe('And when the state changes', () => {
		it('should draw the Begin screen', () => {});
		it('should draw the Exit screen', () => {});
		it('should draw the New Game screen', () => {});
		it('should draw the Ready screen', () => {});
		it('should draw the Play Game screen', () => {});
		it('should draw the Paused screen', () => {});
		it('should draw the Finished screen', () => {});
	});

});