/* stuff here */

body {
    padding: 0 1rem 0 1rem;
}

.field {
    display: flex;
    flex-direction: row;
    gap: 2rem;
    justify-content: space-around;

    div:not(#pitch) {
        flex: 1;
    }
}
